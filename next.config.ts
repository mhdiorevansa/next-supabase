import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["encrypted-tbn0.gstatic.com", "cdn0-production-images-kly.akamaized.net"],
	},
};

export default nextConfig;
